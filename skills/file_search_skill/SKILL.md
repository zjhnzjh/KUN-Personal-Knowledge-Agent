---
name: file-search-skill
description: Locate confirmed local files by filename, metadata, type, tags, and knowledge space. Use when the user wants to find a file rather than answer from its contents.
---

# File Search Skill

1. Search confirmed KUN library metadata with `file.search`.
2. Apply the selected knowledge-space boundary.
3. Return the smallest useful result list with file type and local-copy status.
4. Do not claim that metadata matching proves anything about file contents.

